extends layout

block content
  link(rel='stylesheet', href='/styles/stylesheet2.css')
  h1= title
  p Welcome to #[em WeBlog], using node.js and Express that does Create and Read operations using MongoDB.

  h2 Dynamic content

  if error
    p Error getting dynamic content.
  else

    p The WeBlog Web has the following record counts:

    ul
      li #[strong Blogs:] !{data.blog_count}
      li #[strong Blogger:] !{data.blogger_count}
      li #[strong Genres:] !{data.genre_count}

    